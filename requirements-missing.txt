# Dependências opcionais/missing para o Sistema AutoCura
# Estas são instaladas se disponíveis, mas não são obrigatórias

# Dependências adicionais para recursos avançados
requests
prometheus-client>=0.16.0
psutil>=5.9.0

# Dependências para exportação de métricas
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
opentelemetry-exporter-prometheus>=0.40b0

# Dependências para visualização (opcionais)
plotly>=5.14.0
dash>=2.10.0

# Dependências para ML avançado (opcionais)
torch>=2.0.0
transformers>=4.30.0
accelerate>=0.20.0 