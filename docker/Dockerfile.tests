FROM autocura/base:latest

# Copiar código fonte dos testes
COPY tests /app/tests

# Expor porta (opcional, se necessário)
# EXPOSE 8000

# Comando para executar os testes
CMD ["pytest", "/app/tests"] 